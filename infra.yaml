---
  - name: Initial setup
    hosts: all
    roles:
      - init

  - name: DNS Server
    hosts: dns_server
    roles:
      - bind

  - name: Database Server
    hosts: db_servers
    roles:
      - mysql

  - name: Web Server
    hosts: web_servers
    roles:
      - agama
      - uwsgi
      - nginx 